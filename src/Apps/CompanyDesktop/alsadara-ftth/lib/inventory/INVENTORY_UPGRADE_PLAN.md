# خطة تطوير نظام المخازن والمبيعات والمشتريات المتكامل
> آخر تحديث: 2026-05-09
> الحالة: المرحلة 1-5 ✅ — التالي: المرحلة 6 (ميزات متقدمة اختيارية)

---

## الوضع الحالي (ما هو موجود ويعمل)

| العنصر | الحالة | ملاحظات |
|--------|--------|---------|
| CRUD مستودعات | ✅ مكتمل | |
| CRUD مواد + تصنيفات | ✅ مكتمل | |
| CRUD موردين | ✅ مكتمل | أساسي — بدون رصيد أو كشف حساب |
| أوامر شراء | ✅ مكتمل | بدون قيد محاسبي تلقائي |
| أوامر بيع | ✅ مكتمل | اسم العميل نص فقط — بدون كيان عميل |
| صرف فنيين + إرجاع | ✅ مكتمل | |
| تتبع المخزون + تكلفة متوسطة | ✅ مكتمل | |
| حركات مخزنية (audit trail) | ✅ مكتمل | |
| تقارير أساسية (3 تقارير) | ✅ مكتمل | ملخص + تقييم + عُهد فنيين |
| نظام محاسبي (شجرة حسابات + قيود + صناديق) | ✅ مكتمل | **مستقل — سيتم الربط فقط** |
| `JournalEntryId` في PurchaseOrder و SalesOrder | ⚠️ FK موجود | لكن لا يُنشئ قيد تلقائياً |

---

## ملفات النظام الحالية

### Backend
| الملف | السطور | الوصف |
|-------|--------|-------|
| `Controllers/InventoryController.cs` | ~3,042 | كل الـ endpoints |
| `Entities/Inventory.cs` | ~494 | كل الكيانات |
| `Entities/Accounting.cs` | — | النظام المحاسبي (لن يُعدّل) |
| `Controllers/AccountingController.cs` | ~6,193 | المحاسبة (لن يُعدّل) |

### Frontend (Flutter)
| الملف | الوصف |
|-------|-------|
| `inventory/inventory_page.dart` | الصفحة الرئيسية (dashboard) |
| `inventory/models/inventory_models.dart` | كل الـ models (~911 سطر) |
| `inventory/services/inventory_api_service.dart` | خدمة API (~447 سطر) |
| `inventory/utils/format_utils.dart` | أدوات تنسيق |
| `inventory/pages/warehouses_page.dart` | المستودعات |
| `inventory/pages/materials_page.dart` | المواد |
| `inventory/pages/suppliers_page.dart` | الموردين |
| `inventory/pages/purchases_page.dart` | المشتريات |
| `inventory/pages/purchase_form_page.dart` | نموذج شراء |
| `inventory/pages/sales_page.dart` | المبيعات |
| `inventory/pages/sale_form_page.dart` | نموذج بيع |
| `inventory/pages/dispensing_page.dart` | صرف الفنيين |
| `inventory/pages/dispensing_form_page.dart` | نموذج صرف |
| `inventory/pages/stock_levels_page.dart` | مستويات المخزون |
| `inventory/pages/movements_page.dart` | الحركات |
| `inventory/pages/inventory_reports_page.dart` | التقارير |

---

# ══════════════════════════════════════════════════
#  المرحلة 1: الكيانات والبنية التحتية (Backend)
# ══════════════════════════════════════════════════

## 1.1 — كيان عملاء المخازن `InventoryCustomer` ✅
- [x] إنشاء الكيان في `Inventory.cs` ✅
- [x] إضافة DbSet + QueryFilter + EF Configuration ✅
- [x] إضافة Repository في UnitOfWork ✅

## 1.2 — تطوير كيان المورد `Supplier` ✅
- [x] إضافة حقول: SupplierType, CreditLimit, TotalPurchases, TotalPayments, Balance, AccountId ✅

## 1.3 — كيان الفاتورة `Invoice` ✅
- [x] إنشاء الكيان في `Inventory.cs` — كل الحقول المخططة + DbSet + QueryFilter + EF Config + Repository ✅

## 1.4 — كيان بند الفاتورة `InvoiceItem` ✅

- [x] إنشاء الكيان + DbSet + QueryFilter + EF Config + Repository ✅

## 1.5 — كيان سند القبض / الصرف `PaymentVoucher` ✅

- [x] إنشاء الكيان + DbSet + QueryFilter + EF Config + Repository ✅

## 1.6 — كيان المرتجع `ReturnOrder` ✅

- [x] إنشاء الكيان + DbSet + QueryFilter + EF Config + Repository ✅

## 1.7 — كيان بند المرتجع `ReturnOrderItem` ✅

- [x] إنشاء الكيان + DbSet + QueryFilter + EF Config + Repository ✅

## 1.8 — تطوير Enums ✅

- [x] 11 enum جديد في `Enums.cs` ✅
- [x] تطوير `JournalReferenceType` (+6 قيم: SalesInvoice..SupplierPayment) ✅
- [x] تطوير `StockMovementType` (+2: SalesReturn, PurchaseReturn) ✅

## 1.9 — Migration ✅

- [x] إنشاء Migration: `AddInventoryUpgrade_CustomersInvoicesVouchersReturns` ✅
- [ ] تطبيق Migration على قاعدة البيانات (عند النشر)

## 1.10 — كيان ربط الحسابات `InventoryAccountMapping` ✅

- [x] إنشاء الكيان + DbSet + QueryFilter + EF Config + Repository ✅

---

# ══════════════════════════════════════════════════
#  المرحلة 2: القيود المحاسبية التلقائية (Backend)
# ══════════════════════════════════════════════════

> **مبدأ**: الربط مع النظام المحاسبي الموجود — لا تعديل على AccountingController أو Accounting.cs

## 2.0 — حسابات افتراضية (Auto-seed) ✅

- [x] `POST /accounts/seed` — بذر 8 حسابات + ربطها بـ InventoryAccountMapping ✅
- [x] `GET /accounts/mappings` — جلب ربط الحسابات الحالي ✅

## 2.1 — قيد فاتورة شراء نقد (سيُنفذ ضمن endpoint تأكيد الفاتورة - المرحلة 3)
- [ ] عند تأكيد فاتورة شراء نقد:
```
مدين: 1130 المخزون ← بمبلغ الصافي
دائن: 1110 الصندوق ← بمبلغ الصافي
```

## 2.2 — قيد فاتورة شراء آجل
- [ ] عند تأكيد فاتورة شراء آجل:
```
مدين: 1130 المخزون ← بمبلغ الصافي
دائن: 2110 ذمم دائنة (حساب المورد) ← بمبلغ الصافي
```
- [ ] تحديث رصيد المورد: `Balance += NetAmount`

## 2.3 — قيد فاتورة بيع نقد
- [ ] عند تأكيد فاتورة بيع نقد:
```
قيد 1 (الإيراد):
  مدين: 1110 الصندوق ← بمبلغ الصافي
  دائن: 4100 إيرادات المبيعات ← بمبلغ الصافي

قيد 2 (التكلفة):
  مدين: 5100 تكلفة البضاعة المباعة ← بإجمالي تكلفة المواد
  دائن: 1130 المخزون ← بإجمالي تكلفة المواد
```

## 2.4 — قيد فاتورة بيع آجل
- [ ] عند تأكيد فاتورة بيع آجل:
```
قيد 1 (الإيراد):
  مدين: 1140 ذمم مدينة (حساب العميل) ← بمبلغ الصافي
  دائن: 4100 إيرادات المبيعات ← بمبلغ الصافي

قيد 2 (التكلفة):
  مدين: 5100 تكلفة البضاعة المباعة ← بإجمالي تكلفة المواد
  دائن: 1130 المخزون ← بإجمالي تكلفة المواد
```
- [ ] تحديث رصيد العميل: `Balance += NetAmount`

## 2.5 — قيد سند قبض من عميل
- [ ] عند إنشاء سند قبض:
```
مدين: 1110 الصندوق ← بالمبلغ
دائن: 1140 ذمم مدينة (حساب العميل) ← بالمبلغ
```
- [ ] تحديث رصيد العميل: `Balance -= Amount`
- [ ] إذا مرتبط بفاتورة: تحديث `PaidAmount` و `RemainingAmount` و `Status`

## 2.6 — قيد سند صرف لمورد
- [ ] عند إنشاء سند صرف:
```
مدين: 2110 ذمم دائنة (حساب المورد) ← بالمبلغ
دائن: 1110 الصندوق ← بالمبلغ
```
- [ ] تحديث رصيد المورد: `Balance -= Amount`

## 2.7 — قيد مرتجع مبيعات
- [ ] عند تأكيد مرتجع مبيعات:
```
قيد 1 (عكس الإيراد):
  مدين: 5300 مردودات المبيعات ← بالمبلغ
  دائن: 1140 ذمم مدينة / 1110 الصندوق ← بالمبلغ

قيد 2 (إرجاع للمخزون):
  مدين: 1130 المخزون ← بتكلفة المواد
  دائن: 5100 تكلفة البضاعة المباعة ← بتكلفة المواد
```
- [ ] إرجاع الكميات للمستودع
- [ ] تحديث رصيد العميل إذا آجل

## 2.8 — قيد مرتجع مشتريات
- [ ] عند تأكيد مرتجع مشتريات:
```
مدين: 2110 حساب المورد / 1110 الصندوق ← بالمبلغ
دائن: 1130 المخزون ← بالمبلغ
```
- [ ] خصم الكميات من المستودع
- [ ] تحديث رصيد المورد إذا آجل

## 2.9 — Helper Methods ✅

- [x] `CreateJournalEntryForInventory()` — إنشاء قيد مرحّل + تحديث أرصدة الحسابات ✅
- [x] `GetMappedAccountId()` — جلب حساب من InventoryAccountMapping ✅
- [x] `GetEntityAccountId()` — جلب حساب العميل/المورد ✅
- [x] `UpdateCashBox()` — تحديث رصيد الصندوق + CashTransaction ✅
- [x] تطوير `GenerateOrderNumber()` — دعم INV, PINV, RV, PV, SR, PR, CU ✅

---

# ══════════════════════════════════════════════════
#  المرحلة 3: الـ API Endpoints (Backend)
# ══════════════════════════════════════════════════

## 3.1 — endpoints العملاء ✅

- [x] `GET /customers` — قائمة + بحث + فلتر (نوع، حالة) ✅
- [x] `GET /customers/{id}` — تفاصيل + ملخص مالي ✅
- [x] `POST /customers` — إضافة عميل (كود تلقائي CU-YYYY-NNNN) ✅
- [x] `PUT /customers/{id}` — تعديل ✅
- [x] `DELETE /customers/{id}` — حذف (فقط إذا رصيده صفر) ✅
- [x] `GET /customers/{id}/statement` — كشف حساب (فواتير + سندات + مرتجعات + رصيد تراكمي) ✅

## 3.2 — تطوير endpoints الموردين

- [ ] `GET /suppliers/{id}/statement` — كشف حساب المورد
- [ ] حقول Supplier الجديدة تعمل تلقائياً مع الفواتير والسندات ✅

## 3.3 — endpoints الفواتير ✅

- [x] `GET /invoices` — قائمة (فلتر: نوع، حالة، تاريخ، عميل/مورد) ✅
- [x] `GET /invoices/{id}` — تفاصيل + بنود + سندات مرتبطة ✅
- [x] `POST /invoices` — إنشاء فاتورة بيع أو شراء (مسودة) ✅
- [x] `POST /invoices/{id}/confirm` — تأكيد شامل ✅:
  - حركات مخزنية (خصم/إضافة)
  - تحديث التكلفة المتوسطة (للشراء)
  - قيد محاسبي تلقائي (إيراد + تكلفة بضاعة)
  - تحديث رصيد العميل/المورد (للآجل)
  - تحديث الصندوق + CashTransaction (للنقد)
- [x] `POST /invoices/{id}/cancel` — إلغاء مسودة ✅ (إلغاء مؤكدة → عبر المرتجعات)
- [ ] `GET /invoices/{id}/print` — بيانات الطباعة

## 3.4 — endpoints سندات القبض والصرف ✅

- [x] `GET /vouchers` — قائمة (فلتر: نوع، كيان، تاريخ) ✅
- [x] `POST /vouchers` — إنشاء سند شامل ✅:
  - قيد محاسبي تلقائي
  - تحديث رصيد العميل/المورد
  - تحديث الصندوق
  - تحديث الفاتورة المرتبطة (PaidAmount, Status)

## 3.5 — endpoints المرتجعات ✅

- [x] `GET /returns` — قائمة (فلتر: نوع، حالة، تاريخ) ✅
- [x] `POST /returns` — إنشاء مرتجع (مسودة) ✅
- [x] `POST /returns/{id}/confirm` — تأكيد شامل ✅:
  - حركات مخزنية (إرجاع/خصم)
  - قيد محاسبي (مردودات + عكس تكلفة)
  - تحديث الصندوق (إذا نقد)
  - تحديث رصيد العميل/المورد (إذا خصم من الرصيد)

## 3.6 — endpoints التقارير المتقدمة

- [ ] `GET /reports/profit-loss` — الأرباح والخسائر
- [ ] `GET /reports/customer-aging` — أعمار ديون العملاء
- [ ] `GET /reports/supplier-aging` — أعمار ديون الموردين
- [ ] `GET /reports/top-selling` — أكثر المواد مبيعاً
- [ ] `GET /reports/slow-moving` — المخزون الراكد
- [ ] `GET /reports/item-ledger/{id}` — دفتر حركة مادة
- [ ] `GET /reports/daily-sales` — مبيعات اليوم
- [ ] `GET /reports/cash-flow` — التدفق النقدي

---

# ══════════════════════════════════════════════════
#  المرحلة 4: الـ Models + API Service (Flutter)
# ══════════════════════════════════════════════════

## 4.1 — Models جديدة في `inventory_models.dart` ✅

- [x] `InventoryCustomer` model + fromJson ✅
- [x] `Invoice` model + fromJson (مع items و vouchers) ✅
- [x] `InvoiceItemModel` model + fromJson ✅
- [x] `PaymentVoucher` model + `VoucherSummary` + fromJson ✅
- [x] `ReturnOrder` model + fromJson (مع items) ✅
- [x] `ReturnOrderItemModel` model + fromJson ✅
- [x] `StatementEntry` model (بند كشف الحساب) ✅

## 4.2 — Methods جديدة في `inventory_api_service.dart` ✅

- [x] العملاء: getCustomers, getCustomer, createCustomer, updateCustomer, deleteCustomer, getCustomerStatement (6) ✅
- [x] الفواتير: getInvoices, getInvoice, createInvoice, confirmInvoice, cancelInvoice (5) ✅
- [x] السندات: getVouchers, createVoucher (2) ✅
- [x] المرتجعات: getReturns, createReturn, confirmReturn (3) ✅
- [x] الحسابات: seedAccounts, getAccountMappings (2) ✅

---

# ══════════════════════════════════════════════════
#  المرحلة 5: واجهات Flutter (Frontend)
# ══════════════════════════════════════════════════

## 5.1 — صفحات العملاء
- [ ] `pages/customers_page.dart` — قائمة العملاء:
  - جدول: الكود، الاسم، الهاتف، النوع (نقدي/آجل)، الرصيد
  - بحث + فلتر بالنوع والحالة
  - أزرار: إضافة عميل، تصدير
  - الضغط على عميل → صفحة التفاصيل
- [ ] `pages/customer_form_page.dart` — إضافة/تعديل عميل:
  - الاسم، الهاتف، الهاتف2، الإيميل
  - المدينة، المنطقة، العنوان
  - النوع (نقدي/آجل/VIP)، سقف الائتمان
  - الرقم الضريبي، ملاحظات
- [ ] `pages/customer_details_page.dart` — تفاصيل عميل:
  - بطاقة ملخص (إجمالي مبيعات، مدفوعات، رصيد)
  - تبويبات: كشف الحساب | الفواتير | السندات
  - أزرار سريعة: فاتورة جديدة، سند قبض
- [ ] `pages/customer_statement_page.dart` — كشف حساب تفصيلي:
  - فلتر تاريخ (من - إلى)
  - جدول: التاريخ، البيان، المرجع، مدين، دائن، الرصيد
  - الرصيد الافتتاحي + الختامي
  - زر طباعة

## 5.2 — صفحات الفواتير
- [ ] `pages/invoices_page.dart` — قائمة الفواتير:
  - تبويبات: فواتير البيع | فواتير الشراء | المرتجعات
  - جدول: الرقم، التاريخ، العميل/المورد، المبلغ، المدفوع، المتبقي، الحالة
  - فلاتر: تاريخ، حالة، نوع دفع، عميل/مورد
  - بحث برقم الفاتورة
- [ ] `pages/invoice_form_page.dart` — إنشاء فاتورة (بيع أو شراء):
  - **القسم العلوي:**
    - نوع الفاتورة (بيع/شراء) — يحدد عند الفتح
    - اختيار عميل/مورد (dropdown مع بحث + زر إضافة سريعة)
    - اختيار مستودع
    - التاريخ + تاريخ الاستحقاق (يظهر فقط للآجل)
    - نوع الدفع (نقد / آجل / جزئي)
    - اختيار صندوق (يظهر فقط للنقد)
  - **قسم المواد (جدول):**
    - المادة (dropdown مع بحث) + الكمية + السعر + خصم% + المجموع
    - زر إضافة مادة + تحديد متعدد
    - عرض الرصيد المتاح لكل مادة (للبيع)
    - تلقائي: السعر من SellingPrice (بيع) أو CostPrice (شراء)
  - **قسم الحساب (أسفل):**
    - المجموع الفرعي (SubTotal)
    - الخصم العام (نسبة أو مبلغ)
    - الضريبة (نسبة)
    - الصافي النهائي
    - المدفوع (للجزئي) + المتبقي
  - **ملاحظات + حفظ**
- [ ] `pages/invoice_details_page.dart` — تفاصيل فاتورة:
  - بيانات الفاتورة كاملة
  - جدول المواد
  - ملخص مالي
  - أزرار: تأكيد | إلغاء | طباعة | إنشاء مرتجع | سند قبض/صرف
  - سجل المدفوعات (السندات المرتبطة)
- [ ] `pages/invoice_print_page.dart` — معاينة وطباعة:
  - تصميم فاتورة احترافي (شعار الشركة، بيانات العميل/المورد، جدول مواد، المبالغ)
  - طباعة A4 / حراري

## 5.3 — صفحات سندات القبض والصرف
- [ ] `pages/vouchers_page.dart` — قائمة السندات:
  - تبويبات: سندات قبض | سندات صرف
  - جدول: الرقم، التاريخ، الاسم، المبلغ، طريقة الدفع، الصندوق
  - فلاتر: تاريخ، نوع، عميل/مورد
- [ ] `pages/voucher_form_page.dart` — إنشاء سند:
  - نوع السند (قبض/صرف)
  - اختيار عميل (قبض) أو مورد (صرف)
  - عرض الرصيد الحالي
  - المبلغ + طريقة الدفع + الصندوق
  - ربط بفاتورة (اختياري) — dropdown بالفواتير المستحقة
  - ملاحظات

## 5.4 — صفحات المرتجعات
- [ ] `pages/returns_page.dart` — قائمة المرتجعات:
  - تبويبات: مرتجع بيع | مرتجع شراء
  - جدول: الرقم، التاريخ، الفاتورة الأصلية، المبلغ، الحالة
- [ ] `pages/return_form_page.dart` — إنشاء مرتجع:
  - اختيار فاتورة أصلية (dropdown)
  - عرض مواد الفاتورة مع الكمية الأصلية
  - تحديد الكميات المرتجعة لكل مادة
  - سبب الإرجاع
  - طريقة الإرجاع (نقد / خصم من الرصيد)
  - اختيار صندوق (إذا نقد)

## 5.5 — تطوير صفحة الموردين
- [ ] تعديل `suppliers_page.dart`:
  - إضافة عمود الرصيد + النوع
  - الضغط على مورد → صفحة تفاصيل
- [ ] `pages/supplier_details_page.dart` — تفاصيل مورد:
  - بطاقة ملخص مالي
  - تبويبات: كشف الحساب | الفواتير | السندات
  - أزرار سريعة: فاتورة شراء، سند صرف
- [ ] `pages/supplier_statement_page.dart` — كشف حساب مورد

## 5.6 — تطوير التقارير
- [ ] تطوير `inventory_reports_page.dart` بتبويبات:
  - [ ] تبويب: الأرباح والخسائر (جدول + رسم بياني)
  - [ ] تبويب: أعمار ديون العملاء (30/60/90/120+ يوم)
  - [ ] تبويب: أعمار ديون الموردين
  - [ ] تبويب: أكثر المواد مبيعاً (top 10/20)
  - [ ] تبويب: المخزون الراكد
  - [ ] تبويب: دفتر حركة مادة (اختيار مادة → كل الحركات)
  - [ ] تبويب: مبيعات اليوم
  - [ ] تبويب: التدفق النقدي

## 5.7 — تطوير الصفحة الرئيسية (Dashboard)
- [ ] تعديل `inventory_page.dart`:
  - إضافة بطاقات إحصائيات جديدة:
    - مبيعات اليوم (نقد + آجل)
    - إجمالي ذمم العملاء
    - إجمالي ذمم الموردين
    - أرباح اليوم
  - إضافة أقسام جديدة في الشبكة:
    - العملاء
    - الفواتير
    - السندات
    - المرتجعات
  - إضافة رسم بياني: مبيعات آخر 7 أيام (اختياري)
  - تحسين جدول آخر الحركات

## 5.8 — Permissions
- [ ] إضافة صلاحيات جديدة في `permission_registry.dart`:
```
inventory.customers    — إدارة العملاء
inventory.invoices     — الفواتير
inventory.vouchers     — سندات القبض والصرف
inventory.returns      — المرتجعات
inventory.reports.advanced — التقارير المتقدمة
```

---

# ══════════════════════════════════════════════════
#  المرحلة 6: ميزات متقدمة (اختيارية — حسب الأولوية)
# ══════════════════════════════════════════════════

## 6.1 — نظام التقسيط
- [ ] كيان `InstallmentPlan`:
  - InvoiceId, TotalAmount, NumberOfInstallments, InstallmentAmount
  - StartDate, IntervalDays
- [ ] كيان `Installment`:
  - InstallmentPlanId, DueDate, Amount, PaidAmount, Status, VoucherId
- [ ] صفحة إدارة الأقساط + تنبيهات الاستحقاق

## 6.2 — سجل الأسعار
- [ ] كيان `PriceHistory`:
  - InventoryItemId, PriceType (Cost/Selling/Wholesale), OldPrice, NewPrice, ChangedAt, ChangedById
- [ ] تسجيل تلقائي عند تغيير سعر المادة
- [ ] عرض تاريخ الأسعار في صفحة المادة

## 6.3 — الباركود
- [ ] توليد باركود تلقائي عند إضافة مادة (إذا لم يُدخل)
- [ ] طباعة ملصقات باركود (متعددة في صفحة واحدة)
- [ ] (مستقبلي) قراءة باركود بالكاميرا

## 6.4 — الجرد الفعلي
- [ ] كيان `PhysicalInventory`:
  - WarehouseId, StartDate, Status (InProgress/Completed/Cancelled)
- [ ] كيان `PhysicalInventoryItem`:
  - SystemQuantity, ActualQuantity, Variance
- [ ] صفحة: إنشاء جلسة جرد → إدخال كميات فعلية → تقرير فروقات → تعديل تلقائي

## 6.5 — حد الائتمان
- [ ] عند إنشاء فاتورة بيع آجل:
  - التحقق: `customer.Balance + invoiceAmount <= customer.CreditLimit`
  - إذا تجاوز: تحذير مع إمكانية التجاوز (بصلاحية خاصة)

## 6.6 — تنبيهات ذكية
- [ ] مواد وصلت الحد الأدنى (MinStockLevel)
- [ ] فواتير مستحقة اليوم / متأخرة
- [ ] عملاء تجاوزوا سقف الائتمان
- [ ] موردين لهم مستحقات متأخرة

## 6.7 — متعدد العملات (مستقبلي)
- [ ] كيان `Currency` + `ExchangeRate`
- [ ] حقل `CurrencyId` + `ExchangeRate` في Invoice
- [ ] تحويل تلقائي عند إنشاء القيد

---

# ══════════════════════════════════════════════════
#  ترتيب التنفيذ
# ══════════════════════════════════════════════════

```
الأولوية 1 — المرحلة 1 (الكيانات + Migration)
    ↓
الأولوية 2 — المرحلة 2 (القيود المحاسبية التلقائية)
    ↓
الأولوية 3 — المرحلة 3 (الـ API Endpoints)
    ↓
الأولوية 4 — المرحلة 4 (Flutter Models + API Service)
    ↓
الأولوية 5 — المرحلة 5 (واجهات Flutter — صفحة صفحة)
    ↓
الأولوية 6 — المرحلة 6 (ميزات متقدمة — حسب الحاجة)
```

### ترتيب واجهات المرحلة 5:
```
1. العملاء (أساس لكل شيء)
2. الفواتير (الأهم)
3. السندات (مرتبط بالفواتير)
4. المرتجعات
5. تطوير الموردين
6. التقارير المتقدمة
7. الصفحة الرئيسية (dashboard)
8. الصلاحيات
```

---

# ══════════════════════════════════════════════════
#  سجل التحديثات
# ══════════════════════════════════════════════════

| التاريخ | ما تم إنجازه |
|---------|-------------|
| 2026-05-09 | إنشاء الخطة الكاملة |
| 2026-05-09 | المرحلة 1 ✅ — 7 كيانات + 11 enum + تطوير Supplier + Migration + Build 0 errors |
| 2026-05-09 | المرحلة 2 (helpers) ✅ — 5 helper methods + seed endpoint + mappings endpoint |
| 2026-05-09 | المرحلة 3 (endpoints) ✅ — عملاء 6 + فواتير 5 + سندات 2 + مرتجعات 3 = 16 endpoint |
| 2026-05-09 | المرحلة 4 ✅ — 7 models + 18 API methods في Flutter |
| 2026-05-09 | المرحلة 5 ✅ — 10 صفحات Flutter جديدة + تحديث Dashboard (0 analyze errors) |
| | |
