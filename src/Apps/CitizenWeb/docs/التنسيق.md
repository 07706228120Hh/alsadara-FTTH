# دليل تنسيق واجهات بوابة المواطن (CitizenWeb)

## 📋 المبادئ الأساسية

### 1. لا يوجد تمرير (No Scrolling)
- جميع الصفحات يجب أن تتناسب مع حجم الشاشة بدون الحاجة للتمرير
- استخدام `LayoutBuilder` لحساب الأحجام ديناميكياً
- استخدام `Expanded` و `Flexible` لتوزيع المساحة

### 2. المساحة الآمنة (Safe Area) ⚠️ مهم جداً
- **جميع العناصر يجب أن تكون ضمن المساحة المتاحة للشاشة**
- لا نستخدم `AppBar` أو `Scaffold` مع شريط علوي
- نستخدم `SafeArea` لتجنب شريط النظام
- نحسب المساحة المتاحة = ارتفاع الشاشة - padding العلوي والسفلي

```dart
@override
Widget build(BuildContext context) {
  return Scaffold(
    body: SafeArea(
      child: Padding(
        padding: const EdgeInsets.all(16),
        child: Column(
          children: [
            // زر العودة + العنوان (ثابت في الأعلى)
            _buildHeader(),
            const SizedBox(height: 16),
            // المحتوى (يملأ باقي المساحة)
            Expanded(
              child: _buildContent(),
            ),
          ],
        ),
      ),
    ),
  );
}
```

### 3. زر العودة الموحد 🔙
- **موقع ثابت**: أعلى يمين الشاشة (للعربية RTL)
- **حجم ثابت**: 40x40
- **تصميم موحد**: دائرة بيضاء مع ظل خفيف
- **السهم يشير لليمين** [→] لأن التطبيق عربي
- **يظهر في كل الشاشات الداخلية** (ليس في الصفحة الرئيسية)

```dart
Widget _buildHeader() {
  return Row(
    children: [
      // زر العودة (يمين الشاشة في RTL)
      _buildBackButton(),
      const SizedBox(width: 16),
      // عنوان الصفحة
      Expanded(
        child: Text(
          'عنوان الصفحة',
          style: TextStyle(
            fontSize: 20,
            fontWeight: FontWeight.bold,
          ),
        ),
      ),
    ],
  );
}

Widget _buildBackButton() {
  return GestureDetector(
    onTap: () => Navigator.pop(context),
    child: Container(
      width: 40,
      height: 40,
      decoration: BoxDecoration(
        color: Colors.white,
        shape: BoxShape.circle,
        boxShadow: [
          BoxShadow(
            color: Colors.black.withOpacity(0.1),
            blurRadius: 8,
            offset: const Offset(0, 2),
          ),
        ],
      ),
      child: const Icon(
        Icons.arrow_forward_ios,  // سهم لليمين →
        color: Colors.black87,
        size: 20,
      ),
    ),
  );
}
```

### 4. بطاقات الخدمات (Service Cards) ⭐ التصميم المعتمد
```dart
Widget _buildServiceCard(
  BuildContext context, {
  required IconData icon,
  required String title,
  required String subtitle,
  required Color color,
  required VoidCallback onTap,
}) {
  return MouseRegion(
    cursor: SystemMouseCursors.click,
    child: GestureDetector(
      onTap: onTap,
      child: Container(
        decoration: BoxDecoration(
          // خلفية متدرجة بسيطة (بدون دوائر ديكورية)
          gradient: LinearGradient(
            begin: Alignment.topLeft,
            end: Alignment.bottomRight,
            colors: [
              Colors.white,
              color.withOpacity(0.08),
            ],
          ),
          borderRadius: BorderRadius.circular(20),
          border: Border.all(
            color: color.withOpacity(0.25),
            width: 1.5,
          ),
          boxShadow: [
            BoxShadow(
              color: color.withOpacity(0.15),
              blurRadius: 12,
              offset: const Offset(0, 6),
            ),
          ],
        ),
        child: Padding(
          padding: const EdgeInsets.all(16),
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              // الأيقونة
              Container(
                padding: const EdgeInsets.all(14),
                decoration: BoxDecoration(
                  gradient: LinearGradient(
                    begin: Alignment.topLeft,
                    end: Alignment.bottomRight,
                    colors: [color, color.withOpacity(0.7)],
                  ),
                  borderRadius: BorderRadius.circular(14),
                  boxShadow: [
                    BoxShadow(
                      color: color.withOpacity(0.35),
                      blurRadius: 10,
                      offset: const Offset(0, 4),
                    ),
                  ],
                ),
                child: Icon(icon, color: Colors.white, size: 26),
              ),
              const SizedBox(height: 12),
              // العنوان
              Text(
                title,
                style: TextStyle(
                  fontSize: 14,
                  fontWeight: FontWeight.bold,
                  color: color.withOpacity(0.9),
                ),
                textAlign: TextAlign.center,
              ),
              const SizedBox(height: 4),
              // الوصف
              Text(
                subtitle,
                style: TextStyle(
                  fontSize: 11,
                  color: Colors.grey[600],
                  height: 1.2,
                ),
                textAlign: TextAlign.center,
                maxLines: 2,
              ),
              const SizedBox(height: 10),
              // زر "ابدأ"
              Container(
                padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 5),
                decoration: BoxDecoration(
                  color: color.withOpacity(0.1),
                  borderRadius: BorderRadius.circular(20),
                ),
                child: Row(
                  mainAxisSize: MainAxisSize.min,
                  children: [
                    Text('ابدأ', style: TextStyle(fontSize: 11, fontWeight: FontWeight.w600, color: color)),
                    const SizedBox(width: 4),
                    Icon(Icons.arrow_back_ios_new, size: 10, color: color),
                  ],
                ),
              ),
            ],
          ),
        ),
      ),
    ),
  );
}
```

### 5. الأيقونات داخل البطاقات
```dart
Container(
  padding: EdgeInsets.all(14),
  decoration: BoxDecoration(
    gradient: LinearGradient(
      begin: Alignment.topLeft,
      end: Alignment.bottomRight,
      colors: [color, color.withOpacity(0.7)],
    ),
    borderRadius: BorderRadius.circular(14),
    boxShadow: [
      BoxShadow(
        color: color.withOpacity(0.35),
        blurRadius: 10,
        offset: Offset(0, 4),
      ),
    ],
  ),
  child: Icon(icon, color: Colors.white, size: 26),
)
```

---

## 🎨 الألوان الرسمية

| الخدمة | اللون | الكود |
|--------|-------|-------|
| اشتراك جديد | بنفسجي | `Color(0xFF9C27B0)` |
| تجديد الاشتراك | أخضر | `Color(0xFF4CAF50)` |
| طلب صيانة | برتقالي | `Color(0xFFFF9800)` |
| ترقية الباقة | أزرق | `Color(0xFF2196F3)` |
| الإنترنت | أزرق | `Color(0xFF2196F3)` |
| توصيل ماستر كارد | برتقالي | `Color(0xFFFF9800)` |
| المتجر | أخضر | `Color(0xFF4CAF50)` |

---

## 📐 هيكل الصفحات

### الهيكل الأساسي لكل صفحة (مهم جداً)
```dart
class ExamplePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // بدون AppBar!
      body: SafeArea(
        child: Padding(
          padding: const EdgeInsets.all(16),
          child: Column(
            children: [
              // ═══════════════════════════════════════
              // الجزء 1: الهيدر (زر العودة + العنوان)
              // ارتفاع ثابت: ~50px
              // ═══════════════════════════════════════
              _buildHeader(),
              const SizedBox(height: 16),
              
              // ═══════════════════════════════════════
              // الجزء 2: المحتوى (يملأ باقي المساحة)
              // استخدم Expanded ليأخذ كل المساحة المتبقية
              // ═══════════════════════════════════════
              Expanded(
                child: _buildContent(),
              ),
            ],
          ),
        ),
      ),
    );
  }
}
```

### صفحة الخدمات (4 بطاقات)
```dart
LayoutBuilder(
  builder: (context, constraints) {
    final availableHeight = constraints.maxHeight - 100; // AppBar + padding
    final cardHeight = availableHeight / 2;
    final cardWidth = constraints.maxWidth / 2;
    final aspectRatio = cardWidth / cardHeight;
    
    return GridView.count(
      crossAxisCount: 2,
      childAspectRatio: aspectRatio,
      mainAxisSpacing: 16,
      crossAxisSpacing: 16,
      shrinkWrap: true,
      physics: NeverScrollableScrollPhysics(),
      children: [...],
    );
  },
)
```

### صفحة النموذج (مع Stepper)
```dart
Column(
  children: [
    // مؤشر الخطوات (10%)
    _buildStepIndicator(),
    
    // المحتوى (70%)
    Expanded(
      child: _buildStepContent(),
    ),
    
    // أزرار التنقل (20%)
    _buildNavigationButtons(),
  ],
)
```

---

## 🔤 الخطوط والنصوص

```dart
// عنوان رئيسي
Text(
  'العنوان',
  style: TextStyle(
    fontSize: 18,
    fontWeight: FontWeight.bold,
    color: Colors.black87,
  ),
)

// عنوان فرعي
Text(
  'الوصف',
  style: TextStyle(
    fontSize: 14,
    color: Colors.grey[600],
  ),
)

// نص البطاقة
Text(
  'اسم الخدمة',
  style: TextStyle(
    fontSize: 16,
    fontWeight: FontWeight.bold,
    color: color, // لون الخدمة
  ),
)
```

---

## 📱 التجاوب (Responsive)

### استخدام LayoutBuilder
```dart
LayoutBuilder(
  builder: (context, constraints) {
    // حساب الأبعاد المتاحة
    final availableWidth = constraints.maxWidth;
    final availableHeight = constraints.maxHeight;
    
    // تحديد عدد الأعمدة بناءً على العرض
    final columns = availableWidth > 600 ? 3 : 2;
    
    // حساب نسبة العرض للارتفاع
    final aspectRatio = (availableWidth / columns) / (availableHeight / 2);
    
    return GridView.count(
      crossAxisCount: columns,
      childAspectRatio: aspectRatio,
      // ...
    );
  },
)
```

### منع التمرير
```dart
GridView.count(
  shrinkWrap: true,
  physics: NeverScrollableScrollPhysics(),
  // ...
)
```

---

## 🎯 مثال كامل لبطاقة خدمة

```dart
Widget _buildServiceCard({
  required IconData icon,
  required String title,
  required String subtitle,
  required Color color,
  required VoidCallback onTap,
}) {
  return GestureDetector(
    onTap: onTap,
    child: Container(
      decoration: BoxDecoration(
        border: Border.all(color: Colors.black87, width: 2),
        borderRadius: BorderRadius.circular(16),
        gradient: LinearGradient(
          begin: Alignment.topLeft,
          end: Alignment.bottomRight,
          colors: [
            color.withOpacity(0.1),
            color.withOpacity(0.2),
          ],
        ),
      ),
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          // الأيقونة
          Container(
            padding: const EdgeInsets.all(12),
            decoration: BoxDecoration(
              color: color,
              borderRadius: BorderRadius.circular(12),
              boxShadow: [
                BoxShadow(
                  color: color.withOpacity(0.4),
                  blurRadius: 8,
                  offset: const Offset(0, 4),
                ),
              ],
            ),
            child: Icon(icon, color: Colors.white, size: 32),
          ),
          const SizedBox(height: 12),
          // العنوان
          Text(
            title,
            style: TextStyle(
              fontSize: 16,
              fontWeight: FontWeight.bold,
              color: color,
            ),
          ),
          const SizedBox(height: 4),
          // الوصف
          Text(
            subtitle,
            style: TextStyle(
              fontSize: 12,
              color: Colors.grey[600],
            ),
            textAlign: TextAlign.center,
          ),
        ],
      ),
    ),
  );
}
```

---

## ✅ قائمة التحقق للتنسيق

### المساحة والتخطيط
- [ ] الصفحة تستخدم `SafeArea` لتجنب شريط النظام
- [ ] لا يوجد `AppBar` - نستخدم هيدر مخصص
- [ ] الصفحة تتناسب مع الشاشة بدون تمرير
- [ ] استخدام `Expanded` للمحتوى الرئيسي
- [ ] استخدام `LayoutBuilder` للأحجام الديناميكية
- [ ] منع التمرير بـ `NeverScrollableScrollPhysics()`

### زر العودة
- [ ] زر العودة موجود في أعلى يمين الشاشة (RTL)
- [ ] الزر دائري أبيض مع ظل (40x40)
- [ ] الأيقونة `arrow_forward_ios` (سهم لليمين →)
- [ ] الزر يعمل بـ `Navigator.pop(context)`

### البطاقات
- [ ] البطاقات لها إطار أسود (2px)
- [ ] البطاقات لها خلفية متدرجة بلون الخدمة
- [ ] الأيقونات بيضاء على خلفية ملونة مع ظل
- [ ] النصوص واضحة ومقروءة
- [ ] التباعد متناسق (16px بين العناصر)
- [ ] الزوايا دائرية (16px للبطاقات، 12px للأيقونات)

---

## 📁 الملفات المطبق عليها التنسيق

1. `lib/pages/citizen/internet/internet_services_page.dart`
2. `lib/pages/citizen/internet/maintenance_request_page.dart`
3. `lib/pages/citizen/internet/new_subscription_page.dart`
4. `lib/pages/citizen/citizen_dashboard.dart`
5. `lib/pages/landing_page.dart`
